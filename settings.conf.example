IP="127.0.0.1" #IP клиента, с которого снимаем копию
USER="root" #пользователь SSH клиента, с которого снимаем копию
PORT=22 #порт SSH клиента, с которого снимаем копию

SERVER_IP=""
SERVER_USER=""
SERVER_PORT=22

PROJECT="prompart"
BORG_PASSPHRASE="prompart"
DB_NAME=("sitemanager")